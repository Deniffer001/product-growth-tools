{
  "name": "gkit",
  "version": "0.1.0",
  "private": true,
  "description": "Profile-bound, agent-first growth provider CLI.",
  "bin": {
    "gkit": "bin/gkit.js"
  },
  "files": [
    "bin",
    "docs",
    "generated",
    "policy",
    "scripts",
    "sources",
    "src",
    "evals",
    "!**/*.test.ts"
  ],
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "cli": "bun run ./src/cli.ts",
    "check-types": "tsc --noEmit",
    "generate:dataforseo": "bun run ./scripts/generate-dataforseo.ts",
    "generate:dataforseo:check": "bun run ./scripts/generate-dataforseo.ts --check",
    "generate:posthog": "bun run ./scripts/generate-posthog.ts",
    "generate:posthog:check": "bun run ./scripts/generate-posthog.ts --check",
    "test": "vitest",
    "test:once": "vitest run"
  },
  "dependencies": {
    "@standard-schema/spec": "^1.0.0",
    "@valibot/to-json-schema": "catalog:",
    "ajv": "catalog:",
    "argc": "catalog:",
    "valibot": "catalog:",
    "yaml": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "bun-types": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
