/**
 * @input package release metadata
 * @output shared constants for schema and artifact manifests
 * @pos single source for PostHog CLI version without importing argc schema runtime
 */

export const POSTHOG_CLI_VERSION = "0.1.2";
